<h1>All Grocery Lists</h1>

<ul id="lists">
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, grocery_list_path(list.id) %>
    </li>
  <% end %>
</ul>

<p> <%= link_to "Create a new list" , new_grocery_list_path %> </p>
